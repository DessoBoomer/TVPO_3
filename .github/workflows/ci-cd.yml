name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  ci-cd-deploy:
    runs-on: self-hosted

    steps:
      - name: 🔹 Checkout кода
        uses: actions/checkout@v4

      - name: 🐍 Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Установка зависимостей
        run: |
          pip install -r requirements.txt

      - name: 🧪 Запуск тестов
        run: |
          python -m pytest tests/ -v --tb=short

      - name: Deploy
        run: |
          cd ~/myapp
          git pull origin main
          echo "✅ Deployed from local runner"
